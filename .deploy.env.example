# Copy to .deploy.env and fill in. .deploy.env is gitignored.
VPS_HOST=my-vps                 # ssh alias from ~/.ssh/config (or user@host)
COMPOSE_PATH=/opt/taskwarrior-webui
# IMAGE_NAME=taskwarrior-webui  # optional override
